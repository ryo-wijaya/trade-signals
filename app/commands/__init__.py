from app.commands import signals, signals_plus, watchlist, settings, admin, explain, portfolio_analysis, earnings, news, options, deepdive, cheap, opc  # noqa: F401 — triggers registration
from app.commands.registry import dispatch
